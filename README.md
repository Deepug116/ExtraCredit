# Date Processing and Storage Assignment

## How to Run

This program is a basic in-memory key-value database with support for transactions. It was written in Python 3.

To run the code:

1. Make sure Python 3 is installed.
2. Open a terminal in the folder with the .py file.
3. Run the script using:

No extra packages or setup are needed.

## Notes for Improvement

If this were turned into a full assignment, a few things could be improved:
- It should be clear in the instructions that `get()` shouldn’t return uncommitted values.
- It would help to have some starter tests or an auto-grading script.
- Maybe add support for nested transactions or a way to delete keys.
- The interface could be expanded to include a CLI or input/output examples.